# Release Notes

## Unreleased
- `wl init` now inserts a single pointer line to the global `AGENTS.md` and preserves local rules without duplicating guidance.
- `wl init` prompts to choose a workflow setup (none/basic/manual) and only applies the basic workflow when selected.
